# 绿色骰桌与透明骰盅验证

日期：2026-09-21。项目：`D:\MyAgent\Empower\game_mock`。本记录对应用户批准的场景改版，早期卡片版记录保留在 `ui-review.md`。

## 已实现

- 绿色绒布完整桌面、木质桌边、金色印刷下注格、底部筹码架；四类细分玩法全部铺在桌面。
- 透明骰盅由前后玻璃、上口、反光、底座分层组成；三颗骰子位于杯壁之间。
- 骰子 spatial 变体包含六个独立点面，翻滚时能看到不同面；点面互不重复，相对面之和为七。
- 正常流程为摇盅 1.8 秒 → 等待接口 → 按返回点数落定 320ms → 桌面结果。恢复已完成请求直接展示原结果，避免重复开奖。
- 命中高亮取自服务端已下注条目的 payout；旧筹码作为结果展示，不计入下一局投入。新一局首次下注清除旧结果。明细在桌面内展开。

## 执行证据

- `npm run check`：TypeScript 严格检查、前后端构建、31 项测试通过。新增动画状态顺序/后端点数一致性、恢复结果与明细、六面骰子映射三项回归测试。
- `scripts/visual-review.mjs`：13 个状态 × 320/390px，共 26 个检查，无横向溢出或浏览器脚本错误。骰盅静止、摇动、结果、展开明细及其他三个页面均有快照。生成物位于 `dist/visual-review/`。
- `scripts/verify-table-motion.mjs`：四组检查通过。实际 CSS 中，骰盅、三个骰子位置、立体面旋转都随时间变化；骰子所在容器有裁剪边界；减少动态偏好关闭动画；开奖结果在桌面展示且动画停止。
- 人工查看 390px 已选筹码、摇动中的两个时刻，以及 320px 的结果和命中高亮。
- Premium strict 审计通过：0 errors / warnings / unresolved。DESIGN.md 离线 lint 退出 0，仍有前轮已记录的 14 项 frontmatter 颜色引用警告；这些 token 的运行时关系在正文与 WXSS 中维护。
- 未增加项目依赖，未修改积分规则、后端事务或广告发放逻辑。构建产物已经更新，没有新建提交或推送。

## 验证边界

点数与圆角补缝：确认六个独立圆角面在拼接角处有透明切口，绿色来源为后方桌布。已加入六面封闭白色内核，跟随骰体一起旋转；同时缩小点阵内边距和间隙，放大盅内与下注格的点数。重新执行 31 项测试、26 个布局检查和四组动画检查，全部通过；人工复核静止与翻滚截图中的白色拼接角。

后续圆润度调整：立体面圆角由 9rpx 增至 15rpx，平面骰子由 20rpx 增至 28rpx，柔化边线、高光和底部阴影。重新构建，26 个布局状态及四组动画检查通过；人工复核静止和翻滚截图。只调整骰子材质样式，点数与结算逻辑保持原实现。

这里的视觉检查使用实际 WXML、WXSS 和编译控制器状态的 Chromium 代理，不是微信官方渲染器。动画采用 CSS 立体变换和预设轨迹，属于 2.5D 表现，不是物理碰撞模拟。微信 WebView 的真机帧率、立体变换与裁剪、读屏、软键盘及云端广告仍待实际设备与平台配置联调。
