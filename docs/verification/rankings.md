# 胜率榜与流水榜验证

日期：2026-09-21。

用户确认胜率采用整局净盈利口径。榜单统计全部已结算对局：净盈利局数 / 总局数，以及累计下注积分。未玩过的用户显示未上榜；奖励积分不影响排名。服务端返回两份排序结果，客户端记住所选榜单，切换保留邀请和管理状态。

旧钱包首次进入榜单时，按固定局序号范围分页读取历史结果，补算 profitWins。事务检查补算期间没有增加对局，再更新当前钱包的统计与版本，保留余额、流水和原命中局数。历史不完整时拒绝保存补算结果；新对局造成冲突时提示刷新。

验证：

- `npm run check`：类型检查、构建、44 项测试通过。
- 新增覆盖净亏但命中、持平、盈利、未参与、奖励不改变排名、105 局跨页补算、历史缺失、并发新对局、补算只执行一次、切换和刷新失败保留数据。
- `node scripts/visual-review.mjs`：320/390px 共 64 个页面状态检查通过，无水平溢出或脚本错误；包含两个榜单、管理状态及长昵称/长数字压力样例。压力样例只用于布局，不写入游戏数据。
- 已查看 390px 胜率榜和 320px 长数值流水榜截图。
- Premium strict 静态审计无发现，`git diff --check` 无空白错误。

布局为 Chromium 代理渲染，控制器测试使用模拟微信 API，不能替代微信真机、读屏和云数据库联调。首次旧账户补算耗时取决于历史局数，大量历史数据应预先迁移。本次未部署云端、提交或推送。
