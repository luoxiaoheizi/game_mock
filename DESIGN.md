---
version: alpha
name: "骰趣 Dice Club"
description: "微信虚拟积分骰宝，以绿色绒布大桌面、透明骰盅和桌面筹码还原参与感。"
colors:
  primary: "#205C48"
  ink: "#173C31"
  muted: "#5B7066"
  felt: "#123E32"
  background: "#F3F5EF"
  surface: "#FFFFFF"
  soft: "#E8EEE4"
  border: "#D9E2D6"
  gold: "#B88B43"
  gold-soft: "#F3E5C9"
  on-felt: "#F8F3E5"
  on-felt-muted: "#BFD1C3"
  danger: "#A54236"
  danger-soft: "#FFF0EB"
  focus: "#A56B19"
  scrollbar: "#A3B5A4"
  overlay: "rgba(9,29,23,0.68)"
typography:
  sans:
    fontFamily: '-apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", sans-serif'
  display:
    fontFamily: '"STSong", "Songti SC", "SimSun", serif'
  numeric:
    fontFamily: '"DIN Alternate", "Bahnschrift", -apple-system, sans-serif'
rounded:
  DEFAULT: "8px"
  card: "14px"
  dialog: "18px"
spacing:
  page: "16px"
  section: "16px"
components:
  button: {}
  input: {}
  card: {}
  dialog: {}
  dice: {}
---

# 骰趣设计约定

## 产品与风格

用户明确选择微信小程序和简体中文，当前交付为本机试玩与待接入云端代码。面向使用中文微信的休闲玩家；实际发布地区未确定，不推断海外或日本市场需求。四页均为产品界面，任务是选择玩法、确认投入、看结算、领积分和管理好友资料，不做营销落地页。

用户在 2026-09-21 明确否定了工具页面式骰桌，并批准参考澳门骰宝的场景：整张绿色绒布桌面、木质桌边、金色印刷下注格、透明骰盅、三颗可见骰子，以及桌面的实体感筹码。首页采用这一场景方向；钱包、好友和个人页保留清晰账本风格。保留骰趣名称和原生底部导航，不使用现金、兑换、充值或真实收益暗示。

## 运行时映射

权威样式来源为 `apps/miniprogram/styles/tokens.wxss`，本文件镜像其语义，不生成代码。颜色键逐项映射 `--color-{key}`；字体映射 `--font-body`、`--font-display`、`--font-numeric`。圆角映射 `--radius-control/card/dialog`；间距映射 `--space-page/section`。顶部 frontmatter 中长度是 375px 视口下的 CSS 等价值，运行时使用两倍数值的 rpx，以 750 设计宽度缩放。每次修改 token 要同步本文件并检查预览。微信 switch 的 color 属性不接受 CSS 变量时，显式镜像 primary。首页材料是批准的场景例外，由 pages/game/index.wxss 唯一持有：桌面主绿 #08633F、深绿 #03432D、印刷米金 #EAD9A5、木边 #3B251A。玻璃的透明高光、木纹层次和金属反射使用该文件内成组渐变；骰子六面材质由 components/dice/index.wxss 持有，不复制到其他页面。

## 色彩与字形

primary 承担主操作、选中和正向积分；danger 承担失败与负向积分。选中同时用边框和金额文字表达，正负使用符号和明细，不能只靠颜色。gold 只在骰桌边线与筹码上作装饰，不作浅底小字。felt 上使用 on-felt 系列文字。正文采用系统中文无衬线，宋体仅用于骰盘短标题和小型品牌标记；积分、赔率、日期数字使用 numeric 与等宽数字特性。基础字号 28rpx，标题 44rpx，辅助信息 21～25rpx，正文行高 1.55。长昵称可换行，设置行内才允许省略。

## 布局与层次

四页使用连续自然滚动，页面边距约 32rpx、卡片间距 24rpx。首页所有玩法直接铺在同一张桌面：大小与任意豹子三列、出现某点六列、总点数七列两行、指定豹子六列，不再依赖分组选项卡。每格的筹码显示累计投入，选择明细提供显式减筹码与清空。底部投入操作固定显示，正文预留等高空间；微信原生 tabBar 位于页面宿主外，不另造一套导航。弹窗限制最大高度并可滚动，考虑底部安全区。320px 与 390px 视口检查溢出，昵称与积分不能挤掉主要动作。

卡片用轻边线和底色分层，普通列表不加阴影；阴影仅用于骰子、结算弹层与固定操作区域。控件圆角 16rpx，卡片 28rpx，弹层 36rpx。透明骰盅、圆形筹码和骰子立体面是明确的主题例外。当前定义浅色辅助页面和完整绿色骰桌；系统高对比模式保留边界，未承诺独立深色主题。

## 控件和状态

共享按钮、输入、状态行、卡片、弹层由 `app.wxss` 所有。页面只扩展布局，不再复制一套基础控件。原生 button 处理点击、禁用、busy，TDesign 依赖暂保留兼容但本轮四页统一使用同一原生按钮样式。

主操作用 primary，次操作用 secondary，辅助操作用 text-button；移除好友只在管理状态出现，并先说明后果。默认操作高度 88rpx；焦点有可见轮廓，按下轻微位移。加载用稳定状态行及按钮 loading，保留已有数据，避免反复清空。失败要给出可操作的重试入口；成功结算不能因历史记录读取失败而变成“待恢复”。初次加载的积分用破折号，不误显示真实余额为零。

表单标签始终可见，错误就近显示且保留输入。昵称从领域校验获取真实规则，失败时留在弹窗；保存成功后同步排行榜。弹窗背景标记 aria-hidden，内容提供语义和可见关闭动作。微信原生控件的键盘、焦点与读屏仍需真机复核。

玩法、筹码都显示文字，无仅图标操作；骰子点阵由现有 dice 组件绘制；新增 spatial 变体提供六个立体点面，正面展示实际结算值，相对面之和为七。玻璃盅、底座、反光和筹码通过 WXML/WXSS 绘制，未引入素材下载或新依赖。摇盅分为 shaking 1.8 秒、waiting 等待响应、settling 320ms 落定、result 展示。骰子轨迹和旋转仅作 2.5D 动画，不是物理引擎，不参与结算。减少动态偏好关闭骰盅、弹跳和旋转。积分不做循环跳数，背景不做持续动画。

## 文案与业务边界

只使用简体中文任务文案，英文眉题作为次级品牌信息。净积分显式带正负号，返还写明含本金；显示后端返回值，不由页面重新结算。规则由 `packages/domain/index.ts` 所有，签到、广告限额及模拟说明不能仅写进静态设计稿。空态说明下一步，错误给恢复路径，不使用全屏通用“出错了”。

## 维护和验证

交互边界与控件所有权见 `UX-CONTRACT.md`。运行 `npm run check` 验证跨页状态和失败路径；`scripts/visual-review.mjs` 从实际编译控制器取得状态，并复用 WXML/WXSS 生成 Chromium 布局快照。代理渲染器只做离线布局检查，不能替代微信原生渲染、输入法、读屏与广告验证。禁止把静态截图当作真实微信运行结果。

保持骰桌有辨识度、账本易读、操作有明确反馈；不要给每个区域都加高饱和装饰，不要隐藏减筹码和移除好友的唯一入口，不要用弹 Toast 替代字段错误。

## 骰桌开奖反馈

骰子外观按用户后续偏好采用圆润象牙质感：立体面圆角 15rpx、平面骰子圆角 28rpx，使用柔和放射高光、浅色边线和轻凹点数，避免硬描边和厚重台阶阴影。尺寸、点面映射与翻滚轨迹不因材质调整而改变；材质参数由 dice/index.wxss 唯一维护。

点数需要饱满易读：立体面内边距 9rpx、点阵间隙 3rpx，平面骰子内边距 12rpx、间隙 3rpx。六个圆角外表面背后必须有封闭的纯白内核（原尺寸的 94%），补齐圆角切口；不能让桌布颜色从骰体内部透出。内核跟随同一立体父节点旋转，不参与点数映射。

结算结果留在桌面横条，显示三颗骰子的正面点数、大小/豹子、点数和、净变化；点击明细在桌面内展开。仅根据服务端 result.items 的 payout 高亮已下注且命中的格子，不自行计算返还。结果中的筹码是上一局记录，不计入本局投入；再次下注时清除旧结果与高亮。确认原请求时直接展示原开奖结果，不再次摇盅。网络慢时摇盅先停并提示等待确认，按钮保持锁定。
